# Requirements traceability and evidence status

This release file separates implementation evidence from academic evidence. The separate proposal artifact named in the brief was not present in the repository. The BRD Design Architecture document does contain a Section 7 FR1–FR39 table; those identifiers are treated as source-labelled BRD evidence only and are not presented as proof that the missing proposal was read.

| Requirement source | Current implementation/evidence | Status |
|---|---|---|
| BRD Design Architecture screenshots and Section 7 FR table | `docs/design/references/image1.png`–`image24.png`, `docs/design/route-screen-map.md`, Day42 browser index, Day47 native evidence, and the inspected BRD Section 7 FR1–FR39 table | Implementation evidence recorded; pixel identity is not claimed |
| Revised Day39–60 plan | `docs/progress/day-43-60-tracker.md` with per-day status and command evidence | Implementation/testing evidence exists; September 17 closes the final documentation and sanitized-package review |
| Separate proposal FR1–FR39 artifact | Named proposal file unavailable in repository | Unavailable; do not claim the separate proposal was read |
| Web member journeys | `/app/*` routes, live API contracts, Day42 responsive/browser evidence | Verified through Day42 evidence |
| Web operations journeys | `/admin/*`, role guards, attendance/analytics summary, audit and settings flows | Verified in Day42 web evidence and isolated backend tests |
| Native member journeys | `mobile/`, Expo checks/export, Android evidence index, session/QR/renewal/logout captures | Verified for the documented Android emulator scope |
| Academic UAT and research | No supplied five-participant UAT, twenty-response survey, interviews, sources, or competitor evidence | Unavailable; results must remain empty/labelled |

## Route-level review convention

Each screenshot-backed route is marked `Connected`, `Partial`, or `Exception` in the route map. `Connected` means the route uses live backend data and has the documented loading, empty, error, and authorization states; it does not imply pixel identity with the BRD image. Native screens use the separate evidence index because the web browser cannot prove Android rendering.

## BRD Section 7 FR1–FR39 implementation mapping

Source: `FYP Brief BRD - Anh Khoa - Design Architecture.docx`, Section 7, inspected locally on September 16–17. IDs and requirement titles below retain that source's numbering. The separate named proposal is still unavailable, so proposal reconciliation is not claimed. The BRD explicitly specifies Web + Mobile for FR1–FR13 and Web for FR14–FR15; its extracted table does not specify a platform for FR16–FR39. Their actual coverage is recorded without inventing a platform requirement.

Path conventions: backend service files are under `backend/app/services/`; tests are under `backend/app/tests/` (including `integration/`). Mobile paths refer to `mobile/src/app/`. All API paths use `/api/v1`. “Implemented” means the documented MVP behavior exists and has the listed evidence; it does not assert live settlement, external email delivery, physical hardware, full accessibility compliance or human UAT.

| ID / BRD requirement | Web route / API | Actual native coverage | Code and verification | Status / limitation |
|---|---|---|---|---|
| FR1 User registration | `/register`; `POST /auth/register` | No native registration screen | `auth_service.py`; `integration/test_account_journey.py` registration/conflicts | Partial platform: web/backend implemented; mobile registration absent |
| FR2 Email verification | `/verify-email`; `GET /auth/verify-email` | Verified account required at native login; no native verification screen | `auth_service.py`, `auth_repository.py`; account-journey hashed token and unverified-login checks | Partial: web/backend token flow and private development Maildir; external email/native flow absent |
| FR3 User login/logout | `/login`; `POST /auth/login`, `GET /auth/me`; client logout | `login.tsx`, `lib/auth.tsx`: SecureStore restore, logout/restart, 401 clear | Account/security storage tests; [native evidence](../design/evidence/day-47-mobile/README.md) | Implemented for member app and five-role web access; logout clears client credentials, no server token revocation claim |
| FR4 Forgot password and reset | `/forgot-password`, `/reset-password`; auth endpoints | No native reset screen | `auth_service.py`; account-journey one-time reset, neutral response and security limiter tests | Partial platform/delivery: web and private development Maildir only |
| FR5 Profile self-update | `/app/profile`; `PATCH /users/me` | `(member)/profile-edit.tsx` name/phone/password | `user_service.py`, `user_schema.py`; Day42/legacy regression and native profile evidence | Partial: email replacement is rejected by `EMAIL_CHANGE_NOT_ENABLED` pending secure reverification |
| FR6 Role-based access control | Protected member/admin/PT routes; backend role/ownership dependencies | Member-only login and navigation; staff rejected | `core/dependencies.py`; security/lifecycle/export/trainer storage tests; native denial/expiry evidence | Implemented: five persisted roles; mobile operations/PT workspaces intentionally absent |
| FR7 Membership plan display | `/memberships`; `GET /membership-plans` | `(member)/renew/index.tsx` | `membership_plan_service.py`, `membership_repository.py`, plan schemas; demo fixture and native selection | Implemented; six VND packages |
| FR8 Membership purchase | `/memberships/buy/:planId`; `POST /memberships/purchase` | `(member)/renew/confirm.tsx` shared purchase contract | `membership_service.py`; account-journey idempotency/ownership; native success/invoice | Implemented with explicit simulated-payment confirmation |
| FR9 Volume discount calculation | Plans and server-authoritative purchase amount | Shared server amounts displayed in plan/confirmation screens | `membership_service.py`, `models/membership.py`; account-journey discounted amount check; seeded durations/discounts | Implemented; no client-supplied price trusted |
| FR10 Membership renewal/extension | Same purchase flow; coverage start/expiry returned | Renewal selection/confirmation/success | `membership_service.py`; account-journey renewal/idempotency and demo reseed preservation | Implemented; revoked/cancelled restrictions retained |
| FR11 Membership expiry tracking | `/app/dashboard`, profile and CRM | Dashboard/profile current status and expiry | `lifecycle_service.py`; `test_day_21_38_rules.py` expiry boundaries, lifecycle storage | Implemented; Celery synchronization and relative-date demo |
| FR12 Membership freeze/hold | `/app/membership-requests`, `/admin/approvals`; freeze request/decision endpoints | No native freeze-request screen | `lifecycle_service.py`; lifecycle-storage bounds, permissions, persisted decision/audit | Partial platform: web/backend implemented; native absent |
| FR13 Membership cancellation request | `/app/membership-requests`; cancellation endpoint | No native membership-cancellation screen; class cancellation is separate | `lifecycle_service.py`; lifecycle-storage ownership/request/decision checks | Partial platform: web/backend implemented; native absent |
| FR14 Cancellation approval workflow | `/admin/approvals`; manager/admin decision endpoint | Outside native member scope | `lifecycle_service.py`; lifecycle-storage persisted refund/credit/forfeit decisions and audits | Implemented web MVP; refund/credit outcome is recorded, no real payout |
| FR15 Membership revocation | `/admin/members/:id`; admin-only revoke endpoint | Member access denial uses server state | `operations_service.py`; lifecycle-storage admin-only revoke and renewal denial | Implemented with reason/audit |
| FR16 Payment processing or mock payment | Buy/renew and billing routes | Explicit simulated-payment confirmation | `membership_service.py`; account-journey duplicate/payment/invoice transaction checks | Implemented mock-payment alternative; live settlement outside scope |
| FR17 Invoice generation | `/app/billing`; own invoice PDF endpoint | `(member)/invoices.tsx`, renewal success | `invoice_service.py`; account-journey PDF/ownership, demo invoice immutability | Implemented; immutable metadata, generated files excluded from source package |
| FR18 Payment history | `/app/billing`, `/admin/billing` | Invoice history only; no separate native payment-history list | `billing_service.py`, `operations_service.py`; account journey, CSV tests, connected demo | Implemented web; native coverage limited to invoices |
| FR19 Billing history | `/admin/billing`, member CRM detail | Outside native admin scope | `operations_service.py`, paginated billing repositories; filtered export tests and demo | Implemented admin-only customer billing/invoices |
| FR20 Rotating time-limited QR code | `/app/qr`; `GET /attendance/qr-token/me` | `(tabs)/qr.tsx`: expiry/background/foreground/offline guards | `QrTokenService`; attendance-storage rotation/expiry and QR policy tests; final native retry evidence | Implemented; expiry/supersession/replay reduce sharing, no identity/liveness guarantee |
| FR21 QR attendance check-in | Scanner `POST /attendance/check-in`; web QR | Native presents the shared token; scanner performs attendance mutation | `AttendanceService`; attendance-storage device/race/duplicate tests; demo/simulator contract | Implemented simulator/API flow; physical device unverified |
| FR22 QR check-out or session timeout | Device `POST /attendance/check-out`; worker timeout; manual close | Server occupancy/history reflected on native dashboard | `attendance_service.py`, `workers/celery_app.py`; real timeout/repeat checkout/reconciliation tests | Implemented; repeated processing does not close twice |
| FR23 Duplicate check-in prevention | Scanner check-in | Shared backend safeguards apply | Attendance-storage concurrent scans; Redis guards and unique active-session constraint | Implemented; never enforced solely by UI |
| FR24 Gym crowdedness calculation | Dashboard/attendance; `GET /attendance/crowdedness` | Shared dashboard projection | `CrowdednessService`; attendance-storage PostgreSQL/Redis reconciliation | Implemented; PostgreSQL active sessions authoritative |
| FR25 Capacity-based crowdedness thresholds | Occupancy cards and configuration | Dashboard capacity/percentage/level | `attendance_service.py`, `configuration_service.py`; threshold boundary and configuration storage tests | Implemented percentage-based thresholds |
| FR26 Peak hours heatmap | `/admin/attendance`; `/admin/analytics/peak-hours` | Outside native member scope | Attendance/analytics repositories; analytics-storage UTC heatmap/duration tests | Implemented manager/admin 168-cell web heatmap |
| FR27 Class listing | `/app/classes`; `/classes/upcoming` | `(tabs)/classes.tsx` search/day/empty states | `BookingService`, `class_repository.py`; booking storage, Day42 and native evidence | Implemented with capacity and trainer information |
| FR28 Class registration | `/app/classes`; class booking endpoint | Native book confirmation and My Bookings | `BookingService.book`; storage final-place race/duplicate/membership tests | Implemented; class-row locking prevents overbooking |
| FR29 Class cancellation | `/app/bookings`; own booking cancel endpoint | `(member)/bookings.tsx` cancellation/rebook | `BookingService.cancel`; cutoff/ownership/duplicate tests and native evidence | Implemented inclusive configured cancellation cutoff |
| FR30 Class waitlist | Class join/leave, My Bookings and promotion | Native queue/join/leave and booking state | `BookingService._promote`, `NotificationService`; storage deterministic promotion/ineligible-skip/dedupe | Implemented in-app notifications; email is a local development record, external delivery absent |
| FR31 PT assignment | `/admin/classes`, `/admin/pt-assignments`; class/trainer admin APIs | Assigned trainer shown on member class cards | `ClassService`, `TrainerService`; class conflict/deactivation checks, trainer storage and Day42 evidence | Implemented; class scheduling remains admin-only |
| FR32 PT profile card | Classes/public trainers, `/pt/dashboard`; `/trainers`, `/trainers/me` | Member-safe trainer information in class cards | `TrainerService`, `PTCard`; trainer-workspace ownership/role/next-three schedule tests | Implemented lightweight PT workspace; advanced client/payroll/programming deferred |
| FR33 Admin CRM member management | `/admin/members`, `/admin/members/:id` | Outside native admin scope | `operations_service.py`, `operations_repository.py`; exact-filter tests/query review/demo audited revoke | Implemented admin-only CRM |
| FR34 Admin CSV export | CRM/billing export endpoints | Outside native admin scope | `operations_service.py`; configuration-export storage tests exact filters, formula safety, permissions/audits | Implemented; large-export memory scale remains unmeasured |
| FR35 Audit log | `/admin/audit`; `/admin/audit-logs` | Native profile/purchase use shared audited services where applicable | `AuditRepository`; lifecycle/config/export/booking tests and demo | Implemented transactional sensitive-action audit; secrets filtered |
| FR36 Membership expiry notification | Notifications and scheduled 7/3/1-day reminder job | `(member)/notifications.tsx` displays in-app reminders | `NotificationService.send_expiry_reminders`, Celery; reminder dedupe/preferences tests | Implemented in-app/local email records; actual external email delivery absent |
| FR37 Notification preferences | `/app/notifications/preferences`; own preferences API | `(member)/preferences.tsx` | `notification_service.py`; preference-aware reminder/promotion tests and demo fixtures | Implemented supported flags; email opt-in does not prove delivered mail |
| FR38 Admin broadcast announcement | `/admin/broadcasts`; dashboard/notifications | Dashboard active broadcasts | `NotificationService`; broadcast date validation/role tests and demo | Implemented manager/admin web administration and member display |
| FR39 AI chatbot with authenticated context and guardrails | No released chatbot route/API | None | [Feature freeze](feature-freeze.md) and post-release backlog | Deferred by revised release scope; High-priority BRD requirement remains unmet, no supervisor approval inferred |

## Verification and academic distinction

The September 16 isolated PostgreSQL/Redis suite ran 102 tests successfully; [integration report](integration-report.md) records exact commands and warnings. [Day42 web evidence](../design/evidence/day-42/README.md), [native evidence](../design/evidence/day-47-mobile/README.md), [demo script](../demo/demo-script.md) and [rehearsal report](rehearsal-report.md) contain manual scope and outcomes. A passing backend test does not fill an absent native screen or prove external mail delivery.

The partial FR1/2/4/5/12/13 platform or email coverage and deferred FR39 must remain visible in the academic scope discussion. Five-person UAT/SUS-style scores, interviews, survey, literature, competitors and missing proposal reconciliation remain external evidence requirements; [research inventory](research-evidence-inventory.md) and [UAT templates](uat-checklist.md) contain no fabricated results.
