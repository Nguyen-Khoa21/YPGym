# Native member-app evidence

September 16, 2026 working tree based on `fde7f1e`; Android `Medium_Phone_API_36.1` (`emulator-5554`), Expo Go 57.0.9, Expo SDK57. Screenshots are actual 1080×2400 Android captures with synthetic local accounts. The Expo Go settings/refresh overlay is development tooling, not part of the YPGym design. Older `launch/login/after-login*` images are historical attempts and do not establish authenticated completion.

The normal app remained at web/API 5174/8001. Native QA uses the independent `ypgym-demo` project at 55174/58001. Its initial seed and connected HTTP rehearsal passed before these UI interactions; purchase, renewal, promotion/revocation and subsequent native cancellation/rebooking changed only synthetic demo records. `mobile/.env` was preserved: device localhost8001 is reversed to host58001 and device8082 to LAN-bound Metro8082. See `mobile/README.md` for reproduction and mapping cleanup. Windows `sky` capture failed after documented recovery; native ADB screencap/pull avoids other window occlusion without changing OS permissions/settings.

| BRD reference | Native route under `mobile/src/app/` | Evidence | Functional/parity result |
|---|---|---|---|
| Figure19 / `image19.png`: Dashboard | `(member)/(tabs)/dashboard.tsx` | `dashboard-september16.png`, `session-restored-september16.png` | Authenticated real membership/0% occupancy/broadcast; full restart restored SecureStore session and renewed 380-day coverage. Dark/lime cards and four-tab navigation; live data replaces sample values |
| Figure20 / `image20.png`: Renewal selection | `(member)/renew/index.tsx` | `renew-selection-before-polish-september16.png`, `renew-selection-final-september16.png`, `renew-review-september16.png` | Real six VND plans/current coverage; observed clipped mixed-size label fixed with baseline row, final 380-day label renders completely. Native explicit mock-payment confirmation passed |
| Figure21 / `image21.png`: Renewal success | `(member)/renew/success.tsx` | `renew-success-september16.png`, `invoices-september16.png` | Real account-returned invoice `YPG-20260916-19851CAD`, 720,000 VND, expiry October 1, 2027; invoice history and restarted Dashboard agree |
| Figure22 / `image22.png`: QR check-in | `(member)/(tabs)/qr.tsx` | `qr-final-september16.png`, `qr-offline-final-september16.png`, `qr-retry-final-september16.png`, `qr-recovered-final-september16.png` | Real rotating QR/countdown and centered badge. Complete final-code background/offline/retry sequence passed: no QR offline, successful foreground retry restores fresh code; access-summary Retry then clears its separate error. Earlier before-fix captures retain defect evidence |
| Figure23 / `image23.png`: Class booking | `(member)/(tabs)/classes.tsx` | `classes-september16.png`, `classes-empty-september16.png`, `class-booked-september16.png`, `bookings-september16.png`, `booking-cancelled-september16.png` | Persisted joined/waitlist-position2/full-capacity states, Today empty filter, My Bookings, actual cancel/rebook with native confirmation/success |
| Figure24 / `image24.png`: Member profile | `(member)/(tabs)/profile.tsx` | `profile-september16.png`, `profile-final-september16.png` | Actual account/tier/menu navigation; observed left badge fixed with shared centered-pill option and final centered capture verified |

Supporting `bookings-september16.png` and `booking-cancelled-september16.png` show server's 12-hour cutoff, own bookings/waitlist and persisted cancellation. The subsequent native booking returned “Class booked” and restored the Joined state. Unit/build evidence is separate from these rendering/actions.

`operations-role-denied-september16.png` records a real seeded staff login attempt rejected by the member-only Expo app. `logout-confirm-september16.png`, `logged-out-september16.png`, `logout-restart-september16.png`, `session-expired-september16.png` and `expired-session-restart-september16.png` record secure logout and expired-token cleanup; the one-minute expiry was issued only by the isolated loopback test API.

Reference exceptions are explicit: profile uses the authenticated member's initials rather than an invented avatar; VIP/Advance/Normal and real VND plans replace the reference's Pro/USD sample values. Personalization is deferred after Day60. Invoices replace live payment-method management for simulated billing. No fake Help/Support backend or app version2.4.1 is presented. Dark theme, lime states, menu-card hierarchy and bottom navigation are preserved; these captures do not claim pixel identity.

## Manual regression acceptance

1. Sign in as `member@ypgym.dev` with the documented local demo password. Dashboard must show the existing demo state, not fixtures in local UI state. Passed.
2. Navigate QR → background app (Android Home observed) → remove only the test device's API reverse mapping → foreground app. It must show API failure without a QR. Passed against the final code, without Fast Refresh during the sequence.
3. Restore device8001→host58001 and select Code unavailable → Try again. A successful foreground request must restore the live code/countdown; failed requests and background state keep it hidden. Passed; the access summary has an independent Retry which also recovered. Initialization now uses the current clock to enforce cached expiry immediately on mount.
4. View all classes, select Today (empty), return All, open My Bookings, cancel Strength Foundations with confirmation, return and book it again. Passed, real persisted state and success captured.
5. Select a VND plan, explicitly confirm mock payment, verify success invoice, view Invoices and compare renewed expiry with Dashboard after restart. Passed; 720,000 VND and October 1, 2027 are server-backed synthetic results, not live settlement.
6. Force-stop/relaunch Expo Go with a valid token to prove SecureStore restoration. Passed. Logout/confirm/relaunch, operations-role denial and expired-token401 handling remain pending.

Fresh September16 mobile typecheck/lint/two QR policy tests/Android export passed after all visual and clock fixes (session38946, exit0). The unit tests cover expiry/background/error/eligibility display policy; actual native actions above provide separate callback and restoration evidence. No physical phone/iOS, accessibility screen-reader study or research participant results are claimed.
